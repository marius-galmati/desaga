# dependencies
node_modules/

# build outputs
dist/
build/
out/
.next/
.expo/

# caches
.turbo/
.swc/
coverage/
*.tsbuildinfo

# env files — real secrets never committed; only the *.example templates are.
.env
.env.*
!.env.example
!.env.prod.example

# NOTE: packages/db/src/generated IS committed on purpose (kysely-codegen
# output, regenerated in CI to detect drift). Do not add it here.

# os / editor
.DS_Store
Thumbs.db
*.log
